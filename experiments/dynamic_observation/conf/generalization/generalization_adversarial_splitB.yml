# Cross-Scenario Generalization Experiment - Adversarial Bandit - Split B
#
# This experiment evaluates cross-scenario generalization:
# - Train on 7 scenarios (Split B train set - last 7 envs)
# - Test on 7 held-out scenarios (Split B test set - first 7 envs)
#
# Two panes run in parallel:
# | Pane     | Pretrain | Test Learning | Description                    |
# |----------|----------|---------------|--------------------------------|
# | baseline | ❌       | ✅            | Cold start (from scratch)      |
# | both     | ✅       | ❌            | Train + frozen test            |
#
# For finetune, run separately after 'both' completes:
#   tmuxinator start -p conf/generalization/generalization_adversarial_splitB.yml \
#       mode=finetune model_dir=results/generalization/adversarial_splitB_xxx/train
#
# Usage:
#   tmuxinator start -p conf/generalization/generalization_adversarial_splitB.yml
#   tmuxinator start -p conf/generalization/generalization_adversarial_splitB.yml suffix="my_exp"

name: generalization-adversarial-splitB
root: .
startup_window: main

pre_window:
  - cd <%= @settings["project_root"] || "/path/to/project" %>/experiments/dynamic_observation
  - source <%= @settings["project_root"] || "/path/to/project" %>/.venv/bin/activate
  - unset ALL_PROXY all_proxy

<%- model = @settings["model"] || "openrouter/qwen/qwen-2.5-72b-instruct" -%>
<%- reward_model = @settings["reward_model"] || "openrouter/deepseek/deepseek-v3.2" -%>
<%- eta = @settings["eta"] || "10.0" -%>
<%- max_turns = @settings["max_turns"] || "10" -%>
<%- mode = @settings["mode"] || "parallel" -%>
<%- model_dir = @settings["model_dir"] || "" -%>
<%- suffix = @settings["suffix"] || "" -%>
<%- push_db = @settings["push_db"] || "true" -%>
<%- push_db_flag = push_db == "true" ? "--push-to-db" : "" -%>
<%- suffix_flag = suffix != "" ? "--suffix #{suffix}" : "" -%>

windows:
  - main:
      layout: tiled
      panes:
        <%- if mode == "finetune" && model_dir != "" -%>
        # Finetune mode (run after 'both' completes)
        - finetune: uv run python run_generalization_experiment.py --phase finetune --split B --model "<%= model %>" --reward-eval-model "<%= reward_model %>" --bandit-type adversarial --eta <%= eta %> --max-turns <%= max_turns %> --output-dir results/generalization --strategy-cache-dir cache/strategy_embeddings_v3_slim --model-dir "<%= model_dir %>" <%= push_db_flag %> <%= suffix_flag %>
        <%- else -%>
        # Pane 1: Baseline (no pretraining, online learning on test set)
        - baseline: uv run python run_generalization_experiment.py --phase baseline --split B --model "<%= model %>" --reward-eval-model "<%= reward_model %>" --bandit-type adversarial --eta <%= eta %> --max-turns <%= max_turns %> --output-dir results/generalization --strategy-cache-dir cache/strategy_embeddings_v3_slim <%= push_db_flag %> <%= suffix_flag %>

        # Pane 2: Train + Test (frozen policy)
        - both: uv run python run_generalization_experiment.py --phase both --split B --model "<%= model %>" --reward-eval-model "<%= reward_model %>" --bandit-type adversarial --eta <%= eta %> --max-turns <%= max_turns %> --output-dir results/generalization --strategy-cache-dir cache/strategy_embeddings_v3_slim <%= push_db_flag %> <%= suffix_flag %>
        <%- end -%>
